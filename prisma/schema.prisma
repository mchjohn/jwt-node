generator client {
  provider = "prisma-client"
}

datasource db {
  provider = "postgresql"
}

model Account {
  id       String @id @default(uuid()) @db.Uuid
  name     String
  email    String @unique
  password String

  @@map("accounts")
}
